# 이 프로젝트의 작업 기준

2026-09-09 작업 회고를 바탕으로 사용자의 요청에 따라 기록했다.

## 기존 사용 흐름을 보존한다

- 일반 저장은 사용자가 선택한 TXT **한 개만** 생성한다. 검증용 목록, 별칭 파일, JSON을 옆에 자동 생성하지 않는다.
  - 예외: 「6개 모두 저장…」(2026-09-13 사용자 요청)은 보기 목록 6개만 기존 파일명으로 생성한다. 다른 목록을 끼워 넣지 않는다.
- 보기 목록은 전체 Unity / Mono / 레거시 / MBE / IL2CPP / 중첩의 6개를 유지한다. 내부 검증 항목을 UI에 임의로 추가하지 않는다.
- 사용자가 익숙한 중첩 항목명과 기존 저장 파일명을 임의로 바꾸지 않는다.
- 증거·검증·플래그를 추가하라는 요청을 기존 분류 규칙이나 사용 흐름을 교체해도 된다는 뜻으로 해석하지 않는다.
- 새 사용자 지시가 있으면 그것을 우선한다. 위 기준은 추가 승인 절차를 만들기 위한 것이 아니다.

## 판별과 검증

- 원본 화면 이름과 설치 경로는 보존한다. TXT에는 공백·NFC·아포스트로피를 정규화한 nameKey를 출력한다.
- 플레이어별 백엔드·런타임 증거와 게임 단위 분류를 구분한다. 후보 선택의 불확실성은 근거와 신뢰도로 남긴다.
- 분류하지 못한 항목을 원본 스캔에서 조용히 제거하지 않는다. 내부 잔여 분류와 이전 스캔 대비 누락 검사를 유지한다.
- 출력끼리의 등식뿐 아니라 전체 스캔 커버, 실제 설치 경로 보존, 이전 Unity 분류 누락도 확인한다. 범위가 다른 집계는 직접 비교하지 않는다.
- 구조 기반 중첩과 동일 이름 하위 폴더 신호는 별개로 보존한다. 이 때문에 사용자 메뉴나 일반 저장 파일 수를 늘리지 않는다.

## 완료 기준

- 테스트 통과만으로 완료라고 하지 않는다. 변경된 사용 흐름의 실제 결과도 확인한다. 특히 저장 파일 수·내용·파일명, 보기 항목 수와 중첩 항목의 존재를 확인한다.
- 빌드는 원래 프로젝트 출력 경로를 사용한다. 실행 중인 앱이 파일을 잠그면 사용자에게 종료가 필요하다고 알린다. 별도 출력 경로의 실행 파일을 임의로 만들어 넘기지 않는다.
- 확인한 결과와 미확인 사항을 구분한다. 일부 게임 복귀를 전체 원본 목록 복구로 표현하지 않는다.

## 이번 작업에서 반복된 실패

검증 정보를 자동 저장 파일과 UI 항목으로 확대했고, 기존 규칙·파일명을 여러 번 바꾸면서 회귀를 만들었다.
사용자가 요청한 것은 더 많은 기능이 아니라 기존 흐름 안에서 정확히 작동하는 수정이었다.
작업 범위를 좁게 유지하고, 기존 동작을 보존하며, 사용자에게 보이는 결과까지 검증한다.
